# Root .env — frontend environment variables only.
# Copy to .env and fill in your values. Never commit .env.

# Proxy server URL used by the frontend for Live WebSocket mode.
# Change to wss:// if you run the proxy behind TLS in production.
VITE_PROXY_URL=ws://localhost:3001
